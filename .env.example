# Database connection string
# Format: postgresql://[user[:password]@][host][:port]/[dbname]
DATABASE_URL=postgresql://localhost:5432/target_hvac

# Optional: Port for the server (defaults to 5000)
# PORT=5000
